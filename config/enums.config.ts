enum UserRole {
  ADMIN = 'ADMIN',
  MODERATOR = 'MODERATOR',
  PATIENT = 'PATIENT'
}

enum CheckupType {
  EXAMINATION = 'EXAMINATION',
  CONSULTATION = 'CONSULTATION'
}

export default { CheckupType, UserRole }
